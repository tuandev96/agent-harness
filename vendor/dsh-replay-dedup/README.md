# @dsh-external/dsh-replay-dedup

Rate-limit llm-pi-ai unusable-replay-state warnings to one per route/reason per 60s

由 dsh-super-injector dev_scaffold_plugin 生成。

## 构建与注入

```bash
DSH_CHECKOUT=<checkout> bash scripts/build.sh
# 注入器环境内：dev_inject_plugin <本目录>
```
