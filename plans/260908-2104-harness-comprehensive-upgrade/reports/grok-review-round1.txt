I'll review this slice read-only: first the plan, slice readback, and reviewer template, then the uncommitted files and any tests I can run without editing.I'll inventory the slice, read the new skills/hooks/installer wiring, then run the relevant tests read-only.Skills and hook scripts are in; next I’ll read installer wiring, protocol/docs, and the operations tests, then run the hooks read-only.I'll run hook allow/block probes, the installer/package checks, and a secret scan—read-only against the repo, using a temp dir for install.Reviewed **HEAD `12af77fb`** plus the uncommitted slice (skills/hooks/installer/protocol/adapter/package/tests/docs). Read those files, ran typecheck, `operations` tests, `package:check`, a temp `planInstall`+`install`, and hook allow/block probes. Did **not** observe Claude Code / DSH PreToolUse. Did **not** re-run full `npm test` or `npm run build` (dist already contains `SKILL_PREFIXES`).

## AC coverage

| AC | Evidence | Status |
|----|----------|--------|
| **1. Four skills + frontmatter + templates** | `skills/{capture-intent,requirements-spec,plan-mode,review-policy}/SKILL.md` each has `name` + `description` (python frontmatter check `FRONTMATTER_OK`). Templates: `assets/intent-template.md`, `skills/requirements-spec/assets/spec-template.md`, `assets/plan-template.md`, `assets/REVIEW-template.md`. | **PASS** |
| **2. Hooks allow legit + block protected / test-during-fix / prod without `RELEASE_APPROVAL` / secrets in staged diff or command** | Direct `/bin/bash` probes on source scripts (not host wiring). Allow: src edit, `npm test`, `deploy:staging`, `git status`/`commit` clean → exit 0. Block: abs `generated`/`node_modules`/`.env`/`secrets`/`vendor`/`id_rsa` → 2; `HARNESS_TASK_KIND=fix` on `tests/foo.test.mjs` → 2; `wrangler deploy --env production` without approval → 2; staged `AKIA…` and command-string `AKIA…` → 2. With `RELEASE_APPROVAL=TICKET-1` prod deploy → 0. Host PreToolUse: **not observed**. | **PASS** (script semantics; see 🔴/🟡) |
| **3. Installer installs skills+hooks; no second tracker** | Temp `planInstall`+`install` from repo: **42 entries, `missing: []`**, includes four skills + all six `hooks/` files + `AGENTS.md`. Managed block loads the four skills and points at `hooks/` as reference controls. Protocol L5: “Do not initialize a competing progress tracker.” Skills tell the agent to use existing `plans/`. | **PASS** (install path has 🔴 mode bug) |
| **4. Protocol / docs / adapter / package inventory consistent** | Protocol L5 lists all four skills + `hooks/`. Adapter `skills` + `hooksPack`. `scripts/package.mjs` root `hooks` + required `skills/capture-intent\|plan-mode\|review-policy/SKILL.md` and `hooks/README.md`. `docs/installation.md` tree + confirm tests. `npm run package:check` → `INVENTORY_CHECK_PASS`, 157 files. | **PASS** (🟡 table drift in `docs/agents.md`) |
| **5. Tests cover installer inclusion of new paths** | `tests/operations/installer-memory.test.mjs:13-15`. `node scripts/test.mjs --suite operations` → exit 0; TAP **ok 9** `installer includes artifact-chain skills and hooks when present`. Log: `.harness-checks/2026-09-10T13-53-05.134Z-2fd5288e-f4bf-421d-9cfa-f7cb03e1df45/operations.log`. | **PASS** |
| **6. No false ENFORCED/release; ASSISTED honesty** | Protocol L17 `ASSISTED`. Adapter L14-18 `mode:'ASSISTED'`, `nativeConformanceVerified:false`. Test L31 still asserts that. `package:check` `releaseReady: false`. Hooks README: installing ≠ enforcement. Slice readback does not claim UPG/release. | **PASS** |
| **7. No secrets committed** | Slice is uncommitted. Regex scan of 20 slice files: only pattern strings in `hooks/secret-diff.sh` and `scripts/package.mjs`. `package:check` `scan.builtin: NO_MATCH_ON_CONFIGURED_PATTERNS`. | **PASS** |

`npm run typecheck` → exit 0 (this review). Full `npm test` / `npm run build` **not** re-run here.

## Findings

| # | Sev | Finding | Evidence | File:line | Fix |
|---|-----|---------|----------|-----------|-----|
| 1 | 🔴 | Installer writes hook scripts **mode `600`, not executable**. Documented Claude wiring invokes them as commands, so after `install.mjs --apply` they will not run (`Permission denied`), while source copies are `755`. | Temp install: `hookMode: "600"`, `hookExecutable: false`. `atomicWrite` always `openSync(tmp,'wx',0o600)`. Source `ls`: `rwxr-xr-x` on `*.sh`. `settings.example.json` command is `${CLAUDE_PROJECT_DIR}/.agents/hooks/protected-paths.sh` with no `bash` prefix. | `src/core/files.ts:42`; `src/core/installer.ts:89`; `hooks/settings.example.json:8-26`; `hooks/README.md:36-44` | Preserve `+x` for `*.sh` on install **or** prefix every example command with `bash`. Do not ship a settings snippet that execs 0600 files. |
| 2 | 🟡 | `secret-diff` misses **untracked** secrets on `git add` / `git add && git commit` (PreToolUse: cached+worktree `git diff` both empty). | Isolated repo: `untracked_git_add` exit **0**; `untracked_add_and_commit` exit **0**. Staged and command-string cases correctly exit 2. | `hooks/secret-diff.sh:21-33` | Scan `git ls-files --others` / intended add paths, or `git add -n` output, not only `diff`/`diff --cached`. |
| 3 | 🟡 | Default protected globs **do not match relative paths** without a slash before the segment (`generated/out.ts`, `node_modules/pkg/index.js` → 0). Absolute and `./generated/out.ts` → 2. | Python hook matrix, same script. | `hooks/protected-paths.sh:7-16` | Normalize to absolute (e.g. `pwd`/`CLAUDE_PROJECT_DIR`) before `case`, or add unanchored patterns. |
| 4 | 🟡 | Test-edit glob `*test.*` blocks **non-test** files during fix (`docs/latest.md`, `docs/contest.md` → 2). Violates “allow legitimate actions”. | Same matrix, `HARNESS_TASK_KIND=fix`. | `hooks/test-edit-protect.sh:12` | Drop `*test.*`; keep `*/tests/*`, `*.test.*`, `*.spec.*`, `*_test.*`. |
| 5 | 🟡 | All four hooks **fail-open** on invalid JSON (exit 0). | `not-json` → 0 for protected-paths, production-gate, secret-diff. | `hooks/*.sh` (`python3 … \|\| true` then empty → exit 0) | Fail closed on parse error for Bash/Edit matchers, or document fail-open. |
| 6 | 🟡 | Docs inventory drift: shared loader lists four skills; **compatibility table still only `requirements-spec`**. `docs/usage.md` still only that skill. | `docs/agents.md:13-16` vs L7/L30-34. | `docs/agents.md:13-16`; `docs/usage.md:9` | Name sibling skills (or “`.agents/skills/*`”) in the table and usage. |
| 7 | 🟡 | No automated tests for hook allow/block (AC2 is only this review’s manual probes). `test.mjs` fingerprint walk **omits `hooks/`**. | `scripts/test.mjs:29`; no `tests/**` hitting `hooks/*.sh`. | `scripts/test.mjs:29`; missing `tests/operations/hooks-*.test.mjs` | Add fixture tests for the four scripts; include `hooks` in the fingerprint roots. |
| 8 | 🟢 | `RELEASE_APPROVAL` is any non-empty string; production-gate is substring `deploy`+`prod`. README “Ask/block” is block-only. Acceptable for a reference hook if not advertised as a real release token. | Probe: `TICKET-1` allows prod deploy. | `hooks/production-gate.sh:9-14` | Optional: require a ticket-shaped value; rename README to “block”. |
| 9 | 🟢 | `capabilities()` dropped `requirementsSkill` for `skills`+`hooksPack`. No remaining in-repo callers. Private package. | `rg requirementsSkill` → none. | `adapters/portable/index.mjs:5-13` | None required this slice. |

Checked with **0** leftover 🔴 besides #1: no ENFORCED claims in new skills/hooks; adapter observations still `OBSERVATION_NOT_ENFORCEMENT_ATTESTATION`; installer does not create a kanban/ledger; `requirements-spec.zip` is untracked and excluded from the package zip filter.

Host observation that Claude Code/DSH actually invoke these hooks: **NOT_OBSERVED** (same as slice readback). That is not used as PASS for native enforcement.

VERDICT: BLOCK (1 finding)