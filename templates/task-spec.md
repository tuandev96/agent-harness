# Task Spec — {short name}

Fill all 5 sections BEFORE implementation. If the repo has an active
`./plans/` folder, write this spec there — do not start a second tracker.
The task is done only when every acceptance criterion has current evidence.
If a section cannot be filled, the task is not clear enough — ask instead of
guessing.

## 1. Goal
One or two sentences: what and why. Not how.

## 2. Scope
- Paths/files affected (including read-only):
- Out of scope (must not touch):

## 3. Constraints
- Technical (framework, version, style, required pattern):
- Operational (no downtime, no prod data, …):
- Perf/cost if it matters:

## 4. Acceptance Criteria
Each line is one testable “When… then…”. Examples:
- [ ] AC1: `npm test` PASSes (paste real output)
- [ ] AC2: Open `/login`, sign in as test user → welcome screen
- [ ] AC3: `POST /api/x` returns 200 and body has `y` (real curl)

## 5. Checkpoints (long tasks)
Small steps. Each step: what to do + which evidence proves it. Do not start
the next checkpoint without evidence for the current one.

---

## Execution log (append, do not erase)
| # | Step | Evidence (command / output / screenshot) | Result |
|---|------|------------------------------------------|--------|
| 1 |      |                                          | pass/fail |

## Reviewer pass (required for important work)
- Reviewer (clean-context subagent): ____
- Findings: ____
- Fixed: ____

## Final
- Status: done / blocked
- Evidence summary: ____
- If blocked: real error, 2 approaches tried, next proposal:
