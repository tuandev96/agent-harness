name: Router Standard
description: "Self-routed progressive tool disclosure (one bootstrap guide per session, then zero injection; tools unlock by leveling, zero pre-unlock — each stage sees only its own tools; delivery is a gate, not a stage; phase state persisted per session) on native direct-call surface (no PTC/run_code, no pressure sensor). RL sentence persona plus session cwd / OS home so ~ expands against reality."
order: 1
